# ISS Tracker & Crew Database 🚀

Веб-застосунок для відстеження Міжнародної космічної станції (МКС) у реальному часі, візуалізації її орбіти, а також для перегляду детальної інформації про астронавтів та експедиції.

Цей проект розроблено як курсову роботу з веб-програмування.

## 📋 Функціонал

* **Головна панель (Dashboard):** Загальна статистика, 3D-візуалізація МКС, інформація про поточний екіпаж.
* **Інтерактивна карта:** Відстеження положення станції над поверхнею Землі в реальному часі (день/ніч).
* **База даних астронавтів:**
    * Пошук та фільтрація за країною, віком, статтю, статусом тощо.
    * Детальні картки кожного космонавта з біографією та статистикою польотів.
* **Історія експедицій:** Хронологія всіх місій на МКС з переліком екіпажів.
* **Статистика:** Графіки та діаграми розподілу екіпажу за країнами та гендером.

## 🛠 Технологічний стек

### Frontend (Клієнтська частина)
* **React + Vite:** Швидкий бандлер та бібліотека для побудови інтерфейсу.
* **React Router:** Навігація між сторінками.
* **Leaflet / React-Leaflet:** Відображення мапи.
* **Three.js / @google/model-viewer:** Робота з 3D-моделями (`.glb`, `.usdz`).
* **Recharts:** Побудова графіків статистики.
* **CSS Modules:** Стилізація компонентів.

### Backend (Серверна частина)
* **Python 3.12 + Flask:** REST API сервер.
* **SQLite:** База даних для зберігання інформації про екіпажі та експедиції.
* **SQLAlchemy:** ORM для роботи з базою даних.

## 📂 Структура проекту

```text
.
├── Backend_Flask/       # Серверна частина (Python)
│   ├── app.py           # Головний файл запуску API
│   ├── instance/        # База даних SQLite (iss.db)
│   └── templates/       # HTML шаблони (якщо використовуються)
│
├── Frontend_React/      # Клієнтська частина (React)
│   ├── src/
│   │   ├── assets/      # Зображення, 3D моделі, іконки
│   │   ├── components/  # Перевикористовувані компоненти (картки, слайдери)
│   │   ├── pages/       # Основні сторінки (Home, Astronauts, Expeditions)
│   │   ├── ISSTracker/  # Логіка трекера МКС
│   │   └── utils/       # Допоміжні функції
│   ├── index.html       # Вхідна точка додатку
│   └── vite.config.js   # Конфігурація Vite
│
└── README.md            # Документація проекту

```

## 🚀 Як запустити проект

Для запуску вам знадобляться встановлені **Node.js** та **Python**.

### 1. Налаштування Backend

Перейдіть у папку бекенду:

```bash
cd Backend_Flask

```

Рекомендується створити віртуальне середовище (опціонально):

```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/macOS
# або
# venv\Scripts\activate   # Для Windows

```

Встановіть залежності (якщо є файл requirements.txt, або вручну):

```bash
pip install flask flask-sqlalchemy flask-cors

```

Запустіть сервер:

```bash
python3 app.py

```

*Сервер запуститься на http://127.0.0.1:5000*

### 2. Налаштування Frontend

Відкрийте новий термінал та перейдіть у папку фронтенду:

```bash
cd Frontend_React

```

Встановіть залежності (NPM):

```bash
npm install

```

Запустіть режим розробки:

```bash
npm run dev

```

*Застосунок буде доступний за посиланням, яке з'явиться у терміналі (зазвичай http://localhost:5173)*
